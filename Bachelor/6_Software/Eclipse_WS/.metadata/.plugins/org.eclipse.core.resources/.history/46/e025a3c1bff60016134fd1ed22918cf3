#include "TLinHierarchy.h"
#include "CA.h"
#include "CB.h"
#include "TActionHolder.h"
#include "CMPUCalib.h"

using CActionList = TYPELIST_2(CA, CB);
using CSignalFlow = TLinHierarchy<CActionList, TActionHolder>;

int main()
{
	CSignalFlow test;
	test.calcOutput(3);

	const double& a_out = test.CA::getValue();
	const float& b_out  = test.CB::getValue();

	return 0;
}
